{
  "name": "@groups-sdk/integration-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Integration tests for groups-sdk packages",
  "type": "module",
  "scripts": {
    "test": "vitest run --workspace",
    "test:watch": "vitest --workspace",
    "test:permissioned-groups": "vitest run --project permissioned-groups",
    "test:messaging": "vitest run --project messaging",
    "test:example-apps": "vitest run --project example-apps"
  },
  "keywords": [],
  "author": "Mysten Labs",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "testcontainers": "^11.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "@mysten/sui": "^1.39.0"
  }
}

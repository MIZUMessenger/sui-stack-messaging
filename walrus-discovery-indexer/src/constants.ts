// Prefix for message patch identifiers in Walrus quilts.
// Must match MSG_PREFIX in src/services/walrus_sync.rs
export const MSG_PREFIX = 'msg-';

// Source tag value to identify patches from this relayer.
// Must match SOURCE_TAG in src/services/walrus_sync.rs
export const SOURCE_TAG = 'sui-messaging-relayer';
